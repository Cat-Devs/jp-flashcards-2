type User {
  username: ID!
  name: String
  email: String
}

type Card {
  id: ID!
  category: String
  level: Int
  en: String
  jp: String
}

type Query {
  user(username: String!): User
  createUser(username: String!): User
  card(cardId: String!): Card
  cards: [Card]!
}
